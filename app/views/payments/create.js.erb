// Let's keep this around as a reminder that there is a way to submit
// a remote form using Rails.fire BUT that we got into some problems
// with the sumbission looping and did not have time to investigate 
// while working on the MVP

mui.overlay('off')
showToast('Thank You!', <% @message %>, {});
